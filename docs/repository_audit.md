# Repository audit (Phase 0)
- Date: 2026-09-08. Repo was empty except .git + empty cropguard-ai/ (legacy Next.js/FastAPI scaffold, cleared).
- No reusable code; no datasets/checkpoints present.
- Plan: build AgroVision at repo root per spec; models/ externally owned; tracking via file + optional MLflow/TensorBoard.
