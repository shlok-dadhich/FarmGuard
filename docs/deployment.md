# Deployment
pip install -r requirements.txt; streamlit run app.py. CPU default, CUDA auto. Tracking: file backend always; MLflow optional (`pip install mlflow; mlflow ui`); TensorBoard optional (`tensorboard --logdir outputs/tb`).
